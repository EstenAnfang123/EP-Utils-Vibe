<core:FragmentDefinition
	xmlns="sap.m"
	xmlns:core="sap.ui.core"
>
	<Table
		items="{detail>/planEval}"
		id="tablePlanEval"
		growing="true"
	>
		<headerToolbar>
			<Toolbar>
				<content>
					<Title
						text="{i18n>evaluationTableTitle}"
						level="H2"
					/>
					<ToolbarSpacer/>
					<Button
						icon="sap-icon://excel-attachment"
						text="{i18n>export}"
						type="Accept"
						visible="{= !!${detail>/user}}"
						press=".onPlanExportPress"
					/>
					<Button
						icon="sap-icon://synchronize"
						visible="{= !!${detail>/user}}"
						press=".onRefreshDetailPress"
					/>
				</content>
			</Toolbar>
		</headerToolbar>
		<columns>
			<Column width="14em">
				<Text text="{i18n>month}"/>
			</Column>
			<Column hAlign="Center">
				<Text text="{i18n>january}"/>
			</Column>
			<Column hAlign="Center">
				<Text text="{i18n>february}"/>
			</Column>
			<Column hAlign="Center">
				<Text text="{i18n>march}"/>
			</Column>
			<Column hAlign="Center">
				<Text text="{i18n>april}"/>
			</Column>
			<Column hAlign="Center">
				<Text text="{i18n>may}"/>
			</Column>
			<Column hAlign="Center">
				<Text text="{i18n>june}"/>
			</Column>
			<Column hAlign="Center">
				<Text text="{i18n>july}"/>
			</Column>
			<Column hAlign="Center">
				<Text text="{i18n>august}"/>
			</Column>
			<Column hAlign="Center">
				<Text text="{i18n>september}"/>
			</Column>
			<Column hAlign="Center">
				<Text text="{i18n>october}"/>
			</Column>
			<Column hAlign="Center">
				<Text text="{i18n>november}"/>
			</Column>
			<Column hAlign="Center">
				<Text text="{i18n>december}"/>
			</Column>
		</columns>
		<items>
			<ColumnListItem>
				<cells>
					<Text text="{detail>rowTitle}"/>
					<Input
						value="{
							path: 'detail>january',
							type: 'sap.ui.model.odata.type.Decimal',
							formatOptions: {
								minFractionDigits: 0,
								maxFractionDigits: 3,
								preserveDecimals: false								
							}
						}"
						textAlign="Center"
						submit=".onEvalSubmit"
						editable="{= ${detail>name}==='workingTime'}"						
					/>										
					<Input
						value="{
							path: 'detail>february',
							type: 'sap.ui.model.odata.type.Decimal',
							formatOptions: {
								minFractionDigits: 0,
								maxFractionDigits: 3,
								preserveDecimals: false
							}
						}"
						textAlign="Center"
						submit=".onEvalSubmit"
						editable="{= ${detail>name}==='workingTime'}"					
					/>
					<Input
						value="{
							path: 'detail>march',
							type: 'sap.ui.model.odata.type.Decimal',
							formatOptions: {
								minFractionDigits: 0,
								maxFractionDigits: 3,
								preserveDecimals: false
							}
						}"
						textAlign="Center"
						submit=".onEvalSubmit"
						editable="{= ${detail>name}==='workingTime'}"						
					/>
					<Input
						value="{
							path: 'detail>april',
							type: 'sap.ui.model.odata.type.Decimal',
							formatOptions: {
								minFractionDigits: 0,
								maxFractionDigits: 3,
								preserveDecimals: false
							}
						}"
						textAlign="Center"
						submit=".onEvalSubmit"
						editable="{= ${detail>name}==='workingTime'}"					
					/>
					<Input
						value="{
							path: 'detail>may',
							type: 'sap.ui.model.odata.type.Decimal',
							formatOptions: {
								minFractionDigits: 0,
								maxFractionDigits: 3,
								preserveDecimals: false
							}
						}"
						textAlign="Center"
						submit=".onEvalSubmit"
						editable="{= ${detail>name}==='workingTime'}"						
					/>
					<Input
						value="{
							path: 'detail>june',
							type: 'sap.ui.model.odata.type.Decimal',
							formatOptions: {
								minFractionDigits: 0,
								maxFractionDigits: 3,
								preserveDecimals: false
							}
						}"
						textAlign="Center"
						submit=".onEvalSubmit"
						editable="{= ${detail>name}==='workingTime'}"						
					/>
					<Input
						value="{
							path: 'detail>july',
							type: 'sap.ui.model.odata.type.Decimal',
							formatOptions: {
								minFractionDigits: 0,
								maxFractionDigits: 3,
								preserveDecimals: false
							}
						}"
						textAlign="Center"
						submit=".onEvalSubmit"
						editable="{= ${detail>name}==='workingTime'}"						
					/>
					<Input
						value="{
							path: 'detail>august',
							type: 'sap.ui.model.odata.type.Decimal',
							formatOptions: {
								minFractionDigits: 0,
								maxFractionDigits: 3,
								preserveDecimals: false
							}
						}"
						textAlign="Center"
						submit=".onEvalSubmit"
						editable="{= ${detail>name}==='workingTime'}"						
					/>
					<Input
						value="{
							path: 'detail>september',
							type: 'sap.ui.model.odata.type.Decimal',
							formatOptions: {
								minFractionDigits: 0,
								maxFractionDigits: 3,
								preserveDecimals: false
							}
						}"
						textAlign="Center"
						submit=".onEvalSubmit"
						editable="{= ${detail>name}==='workingTime'}"						
					/>
					<Input
						value="{
							path: 'detail>october',
							type: 'sap.ui.model.odata.type.Decimal',
							formatOptions: {
								minFractionDigits: 0,
								maxFractionDigits: 3,
								preserveDecimals: false
							}
						}"
						textAlign="Center"
						submit=".onEvalSubmit"
						editable="{= ${detail>name}==='workingTime'}"						
					/>
					<Input
						value="{
							path: 'detail>november',
							type: 'sap.ui.model.odata.type.Decimal',
							formatOptions: {
								minFractionDigits: 0,
								maxFractionDigits: 3,
								preserveDecimals: false
							}
						}"
						textAlign="Center"
						submit=".onEvalSubmit"
						editable="{= ${detail>name}==='workingTime'}"						
					/>
					<Input
						value="{
							path: 'detail>december',
							type: 'sap.ui.model.odata.type.Decimal',
							formatOptions: {
								minFractionDigits: 0,
								maxFractionDigits: 3,
								preserveDecimals: false
							}
						}"
						textAlign="Center"
						submit=".onEvalSubmit"
						editable="{= ${detail>name}==='workingTime'}"						
					/>
				</cells>
			</ColumnListItem>
		</items>
	</Table>
</core:FragmentDefinition>